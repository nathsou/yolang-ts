
trait PartialEq<Rhs> {
  fn eq(self: Self, other: Rhs) -> bool
}

type Point = {
  x: u32,
  y: u32
}

impl Traits.PartialEq<Point> for Point {
  fn eq(self, other) {
    self.x == other.x && self.y == other.y
  }
}

fn main() {
  let p1 = Point { x: 1, y: 2 }
  let p2 = Point { x: 3, y: 4 }
  
  p1.eq(p2)
}