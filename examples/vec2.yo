
type Vec2 = { x: float, y: float }

fun dot(u: Vec2, v: Vec2) {
  u.x * v.x + u.y * v.y
}

fun `==`(a: Vec2, b: Vec2) {
  (a.x == b.x) and (a.y == b.y)
}

fun main() {
  let u = Vec2 { x: 3.4, y: 4.1 }
  let v = Vec2 { x: 7.7, y: 11.881 }

  print(u.dot(v))
}
